async (page) => {
 const errors=[];page.on('pageerror',e=>errors.push(e.message));
 await page.getByRole('button',{name:'开始执行（模拟）',exact:true}).click();
 await page.getByRole('dialog').getByRole('button',{name:'确认',exact:true}).click();
 await page.getByRole('button',{name:'记录参数变更',exact:true}).click();
 await page.getByRole('textbox',{name:'新值',exact:true}).fill('220 / 245 / 260');
 await page.getByRole('textbox',{name:'参数变更原因',exact:true}).fill('校准后将中心温度改为 245 ℃，已复核设备范围。');
 await page.getByRole('dialog').getByRole('button',{name:'确认',exact:true}).click();
 await page.getByRole('button',{name:'模拟实验异常',exact:true}).click();
 await page.getByRole('dialog').getByRole('button',{name:'确认',exact:true}).click();
 if(!await page.getByRole('button',{name:'确认恢复',exact:true}).isDisabled())throw new Error('未处置异常可恢复');
 await page.getByRole('button',{name:'查看异常与处置',exact:true}).click();
 await page.screenshot({path:'output/playwright/do-exception.png'});
 await page.getByRole('button',{name:'提交异常处理',exact:true}).click();
 await page.getByRole('textbox',{name:'处置方案、设备复核与样品检查结果',exact:true}).fill('检查温控接线与校准结果，样品未损坏；设备复核通过，保留异常标记。');
 await page.getByRole('dialog').getByRole('button',{name:'确认',exact:true}).click();
 await page.getByRole('button',{name:'确认恢复',exact:true}).click();
 await page.getByRole('dialog').getByRole('button',{name:'确认',exact:true}).click();
 await page.getByRole('button',{name:'模拟完成实验并采集结果',exact:true}).click();
 await page.getByRole('button',{name:'推进至表征分析',exact:true}).click();
 await page.getByRole('button',{name:'完成实验记录',exact:true}).click();
 await page.getByRole('link',{name:'进入表征分析',exact:true}).click();
 await page.getByRole('checkbox',{name:'曲线平滑',exact:true}).check();
 await page.getByRole('button',{name:'应用处理',exact:true}).click();
 await page.getByRole('tab',{name:'计算 vs 实验',exact:true}).click();
 await page.getByRole('button',{name:'生成实验报告',exact:true}).click();
 await page.getByRole('button',{name:'确认实验结论',exact:true}).click();
 await page.getByRole('dialog').getByRole('button',{name:'确认',exact:true}).click();
 await page.getByRole('button',{name:'用于模型校准 / 重新计算',exact:true}).click();
 await page.getByRole('dialog').getByRole('button',{name:'确认',exact:true}).click();
 await page.getByRole('link',{name:'打开下游草稿',exact:true}).waitFor();
 await page.getByRole('button',{name:'补充文献研究',exact:true}).click();
 await page.getByRole('dialog').getByRole('button',{name:'确认',exact:true}).click();
 await page.getByRole('link',{name:'打开下游草稿',exact:true}).nth(1).waitFor();
 await page.locator('main').evaluate(el=>el.scrollTop=0);
 await page.screenshot({path:'output/playwright/do-analysis.png'});
 const url=page.url();await page.reload();
 await page.getByRole('heading',{name:'实验表征分析',exact:true}).waitFor();
 await page.getByRole('link',{name:'打开下游草稿',exact:true}).nth(1).waitFor();
 const state=await page.evaluate(()=>JSON.parse(localStorage.getItem('ai4s-do-space-v1')).state);
 const t=state.tasks.find(t=>t.name.startsWith('演示验收：'));
 const r=state.results.find(r=>r.id===t.resultId);
 return {url,errors,task:{status:t.status,changes:t.changes.length,exceptions:t.exceptions},result:{confirmed:r.confirmed,operations:r.operations},transfers:state.transfers.filter(h=>h.resultId===r.id).map(h=>({direction:h.direction,href:h.href}))};
}
