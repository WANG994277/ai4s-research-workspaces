# 算空间重构验证记录

日期：2026-09-21。依据：AI4S 算空间 PRD V1.0；参考 UI 用于布局参考，配色及交互规则以 PRD 为准。

## 已通过的自动检查
- `pnpm run ts-check`：全项目 TypeScript 检查通过。
- `pnpm run lint:build`：全项目 ESLint 检查通过。将定制规则的文件范围对齐 Next.js 插件覆盖范围，修复 `.cjs` 脚本触发的 `import/no-cycle` 插件缺失配置问题。
- `pnpm build`：生产构建通过，包含算空间、读空间与做空间动态路由。
- `scripts/check-compute-space.ts`：确认前阻断、创建后输入版本引用、方案快照隔离、生命周期、克隆、许可阻断、重试日志、数据与方案血缘。
- `scripts/check-compute-prd.ts`：12 页、29 条需求、全部已有 PRD 锚点可解析、修订进入导出、完整保留源 PRD 第 0–33 章。
- `scripts/check-compute-handoff.ts`：地学、材料、生物交接的样品与表征配置正确，参数、课题与来源证据保留。

## 已完成的浏览器操作
- 首页输入问题 → Agent 分阶段生成 → 四项人工确认 → 新建正式计算任务。
- 任务分配资源 → 多次推进 → 结果生成 → 查看 7 类产物；刷新后完成状态、版本与人工确认保留。
- 从结果生成实际实验方案草稿，并在做空间查看携带的目标、假设、参数、步骤和来源。发现默认样品映射问题后，修复桥接并加入三场景回归测试。
- 数据处理 → 处理中刷新 → 恢复并完成 → 保存 V4 → “用于模型训练”正确绑定 V4。
- 创建模型训练 → 刷新 → 恢复并完成 → 固定测试集评价 → 显示可导出报告、误差与科学约束。
- 科研工具：MATLAB 过期许可禁用调用；新工具接入 → 参数/输入输出 Schema → 环境与许可 → 本地调试通过 → 人工确认上架。
- 设计筛选：生成 12 个候选，显示约束通过/淘汰及 4 个非支配候选，切换帕累托视图。
- PRD 右侧检视：修改与显式保存，刷新后恢复内容，恢复本条原文。
- 1934、1440 和 390 像素视口检查，工作区能换行/折叠；密集表格使用独立横向滚动容器。

## 演示边界
数据处理、计算、模型训练和评估、工具接口联调均为 Mock 演示。科学数值不代表真实求解结果。各场景结果使用对应指标；未知领域展示结构化产物和待人工补充的评价内容。详见 `demo-guide.md`。
