# 做空间重构实施计划

依据：AI4S_做空间_PRD_V1.0.md。用户已授权根据 PRD 自行调整参考图并使用 mock 数据完成全部演示流程。

目标：在现有 Next.js 原型中建立可追溯的实验设计、审核、执行和结果闭环。

设计：沿用平台 Microsoft YaHei 字体与朱丹红 #b4232d，白色表面、灰色边框、绿色成功状态；主输入优先，首页无统计卡。设计/编排/分析采用可响应三栏；台账采用筛选表格与详情抽屉。保留读空间和算空间既有改动。

实现顺序：
- [x] 建立独立做空间数据模型、种子、持久化状态与关键业务守卫；先覆盖未定版禁止执行、预约冲突、异常阻断、版本保留测试。
- [x] 新增 /do-space 路由与共用组件；接入现有导航和课题上下文；旧入口保留参数重定向。
- [x] 首页、上下文引用、模板、三栏 Agent、参数来源确认、多方案比较、风险检查、版本差异、会签退回与定版。
- [x] 样品/设备台账、预约日历、任务编排、人工确认、执行步骤、参数变更及异常处置。
- [x] 原始数据预览、清洗/转换、曲线/图像/多样品比较、结果 Artifact、人工结论与读/算空间回流。
- [x] PRD 检查抽屉、编辑与导出、演示重置、演示说明和覆盖清单。
- [x] 类型/静态检查、关键状态测试、浏览器完整流程及截图。

文件边界：src/components/do-space-v2/{types,seed,store,ui,shell,home,plans,resources,execution,analysis,requirements}.tsx/ts；模块样式 do-space.css；src/app/(app)/do-space/[[...segments]]/page.tsx；scripts/check-do-space.ts；docs/do-space/。

验收：从新建目标生成方案、确认 AI 参数、会签、人工定版、关联样品设备、确认预约、审核并下发，执行异常暂停处置后继续，采集数据、生成报告、确认结论、回流生成下游草稿。刷新保持状态。设备维护或预约冲突、未定版、未确认参数和未解决异常均阻断相关操作。
