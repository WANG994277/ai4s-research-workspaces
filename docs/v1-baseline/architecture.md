# 重构记录

需求：严格实现本次V1.0功能与权限，允许重构视觉。

复用选型：继续使用现有Next.js 16/React 19、Radix Dialog、Lucide、已有图谱路径算法；无需新增依赖。拒绝引入新的UI框架和自建弹窗焦点系统，以减少迁移风险。旧业务页面只在符合新对象边界时复用算法/组件，不沿用旧导航及业务流程。

实现：独立v1组件目录隔离历史业务，统一本地原型对象和权限策略，canonical路由与旧地址重定向；不修改已有旧版localStorage。未来替换本地存储为API时复用对象和操作校验，但必须由后端认证授权。

边界：本地示例并非生产数据。AI、检索、AI中台、设备、外部项目系统用可核验模拟状态与未连接反馈，不宣称真实调用。没有后台管理专项PRD，只保留IA规定治理入口与已有对象管理，不发明完整后台模块。
